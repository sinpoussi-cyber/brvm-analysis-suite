name: BRVM Analysis Suite V9.1 - 30 Clés API (CORRIGÉ)

on:
  schedule:
    # Exécution TOUS LES JOURS à 06h00 GMT
    - cron: '0 6 * * *'
  workflow_dispatch:  # Permet lancement manuel

jobs:
  brvm-analysis:
    runs-on: ubuntu-latest
    timeout-minutes: 180  # 3 heures max
    
    steps:
      - name: 🔥 Checkout du code
        uses: actions/checkout@v3
      
      - name: 🐍 Configuration Python 3.11
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
          cache: 'pip'
      
      - name: 📦 Installation des dépendances
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      
      - name: 🌐 Installation Chrome/Chromedriver (MÉTHODE CORRIGÉE)
        run: |
          sudo apt-get update
          sudo apt-get install -y wget unzip
          
          # ✅ CORRECTION: Installation Chrome (méthode moderne)
          wget -q https://dl.google.com/linux/direct/google-chrome-stable_current_amd64.deb
          sudo apt-get install -y ./google-chrome-stable_current_amd64.deb
          
          # Installation de Chromedriver
          CHROME_VERSION=$(google-chrome --version | grep -oP '\d+\.\d+\.\d+')
          CHROME_MAJOR_VERSION=$(echo $CHROME_VERSION | cut -d. -f1)
          
          # Télécharger Chromedriver compatible
          wget -q "https://storage.googleapis.com/chrome-for-testing-public/${CHROME_VERSION}.0/linux64/chromedriver-linux64.zip" -O chromedriver.zip || \
          wget -q "https://chromedriver.storage.googleapis.com/LATEST_RELEASE_${CHROME_MAJOR_VERSION}" -O version.txt && \
          CHROMEDRIVER_VERSION=$(cat version.txt) && \
          wget -q "https://chromedriver.storage.googleapis.com/${CHROMEDRIVER_VERSION}/chromedriver_linux64.zip" -O chromedriver.zip
          
          unzip -q chromedriver.zip
          if [ -d "chromedriver-linux64" ]; then
            sudo mv chromedriver-linux64/chromedriver /usr/local/bin/
          else
            sudo mv chromedriver /usr/local/bin/
          fi
          sudo chmod +x /usr/local/bin/chromedriver
          
          # Nettoyage
          rm -rf chromedriver-linux64 chromedriver.zip version.txt google-chrome-stable_current_amd64.deb
          
          echo "✅ Chrome et Chromedriver installés"
          google-chrome --version
          chromedriver --version
      
      - name: 🔐 Configuration des secrets de base de données
        env:
          DB_NAME: ${{ secrets.DB_NAME }}
          DB_USER: ${{ secrets.DB_USER }}
          DB_PASSWORD: ${{ secrets.DB_PASSWORD }}
          DB_HOST: ${{ secrets.DB_HOST }}
          DB_PORT: ${{ secrets.DB_PORT }}
        run: |
          echo "✅ Secrets DB configurés"
          echo "Heure d'exécution: $(date '+%Y-%m-%d %H:%M:%S UTC')"
          echo "Version: 9.1 (Corrigée)"
          echo "Nombre de clés API: 30"
      
      - name: 📊 ÉTAPE 1 - Collecte de Données
        env:
          DB_NAME: ${{ secrets.DB_NAME }}
          DB_USER: ${{ secrets.DB_USER }}
          DB_PASSWORD: ${{ secrets.DB_PASSWORD }}
          DB_HOST: ${{ secrets.DB_HOST }}
          DB_PORT: ${{ secrets.DB_PORT }}
        run: |
          echo "🚀 Démarrage de la collecte de données..."
          python data_collector.py
          echo "✅ Collecte terminée"
      
      - name: 📈 ÉTAPE 2 - Analyse Technique
        env:
          DB_NAME: ${{ secrets.DB_NAME }}
          DB_USER: ${{ secrets.DB_USER }}
          DB_PASSWORD: ${{ secrets.DB_PASSWORD }}
          DB_HOST: ${{ secrets.DB_HOST }}
          DB_PORT: ${{ secrets.DB_PORT }}
        run: |
          echo "🚀 Démarrage de l'analyse technique..."
          python technical_analyzer.py
          echo "✅ Analyse technique terminée"
      
      - name: 🔮 ÉTAPE 3 - Prédictions (20 par société)
        env:
          DB_NAME: ${{ secrets.DB_NAME }}
          DB_USER: ${{ secrets.DB_USER }}
          DB_PASSWORD: ${{ secrets.DB_PASSWORD }}
          DB_HOST: ${{ secrets.DB_HOST }}
          DB_PORT: ${{ secrets.DB_PORT }}
        run: |
          echo "🚀 Démarrage des prédictions..."
          python prediction_analyzer.py
          echo "✅ Prédictions terminées (20 jours par société)"
      
      - name: 📄 ÉTAPE 4 - Analyse Fondamentale (30 Clés API)
        env:
          DB_NAME: ${{ secrets.DB_NAME }}
          DB_USER: ${{ secrets.DB_USER }}
          DB_PASSWORD: ${{ secrets.DB_PASSWORD }}
          DB_HOST: ${{ secrets.DB_HOST }}
          DB_PORT: ${{ secrets.DB_PORT }}
          # ✅ VOS 30 CLÉS API GEMINI
          GOOGLE_API_KEY_1: ${{ secrets.GOOGLE_API_KEY_1 }}
          GOOGLE_API_KEY_2: ${{ secrets.GOOGLE_API_KEY_2 }}
          GOOGLE_API_KEY_3: ${{ secrets.GOOGLE_API_KEY_3 }}
          GOOGLE_API_KEY_4: ${{ secrets.GOOGLE_API_KEY_4 }}
          GOOGLE_API_KEY_5: ${{ secrets.GOOGLE_API_KEY_5 }}
          GOOGLE_API_KEY_6: ${{ secrets.GOOGLE_API_KEY_6 }}
          GOOGLE_API_KEY_7: ${{ secrets.GOOGLE_API_KEY_7 }}
          GOOGLE_API_KEY_8: ${{ secrets.GOOGLE_API_KEY_8 }}
          GOOGLE_API_KEY_9: ${{ secrets.GOOGLE_API_KEY_9 }}
          GOOGLE_API_KEY_10: ${{ secrets.GOOGLE_API_KEY_10 }}
          GOOGLE_API_KEY_11: ${{ secrets.GOOGLE_API_KEY_11 }}
          GOOGLE_API_KEY_12: ${{ secrets.GOOGLE_API_KEY_12 }}
          GOOGLE_API_KEY_13: ${{ secrets.GOOGLE_API_KEY_13 }}
          GOOGLE_API_KEY_14: ${{ secrets.GOOGLE_API_KEY_14 }}
          GOOGLE_API_KEY_15: ${{ secrets.GOOGLE_API_KEY_15 }}
          GOOGLE_API_KEY_16: ${{ secrets.GOOGLE_API_KEY_16 }}
          GOOGLE_API_KEY_17: ${{ secrets.GOOGLE_API_KEY_17 }}
          GOOGLE_API_KEY_18: ${{ secrets.GOOGLE_API_KEY_18 }}
          GOOGLE_API_KEY_19: ${{ secrets.GOOGLE_API_KEY_19 }}
          GOOGLE_API_KEY_20: ${{ secrets.GOOGLE_API_KEY_20 }}
          GOOGLE_API_KEY_21: ${{ secrets.GOOGLE_API_KEY_21 }}
          GOOGLE_API_KEY_22: ${{ secrets.GOOGLE_API_KEY_22 }}
          GOOGLE_API_KEY_23: ${{ secrets.GOOGLE_API_KEY_23 }}
          GOOGLE_API_KEY_24: ${{ secrets.GOOGLE_API_KEY_24 }}
          GOOGLE_API_KEY_25: ${{ secrets.GOOGLE_API_KEY_25 }}
          GOOGLE_API_KEY_26: ${{ secrets.GOOGLE_API_KEY_26 }}
          GOOGLE_API_KEY_27: ${{ secrets.GOOGLE_API_KEY_27 }}
          GOOGLE_API_KEY_28: ${{ secrets.GOOGLE_API_KEY_28 }}
          GOOGLE_API_KEY_29: ${{ secrets.GOOGLE_API_KEY_29 }}
          GOOGLE_API_KEY_30: ${{ secrets.GOOGLE_API_KEY_30 }}
        run: |
          echo "🚀 Démarrage de l'analyse fondamentale avec 30 clés API..."
          python fundamental_analyzer.py
          echo "✅ Analyse fondamentale terminée"
      
      - name: 📝 ÉTAPE 5 - Génération des Rapports (Clés non épuisées)
        env:
          DB_NAME: ${{ secrets.DB_NAME }}
          DB_USER: ${{ secrets.DB_USER }}
          DB_PASSWORD: ${{ secrets.DB_PASSWORD }}
          DB_HOST: ${{ secrets.DB_HOST }}
          DB_PORT: ${{ secrets.DB_PORT }}
          # ✅ MÊMES 30 CLÉS (le système utilisera celles non épuisées)
          GOOGLE_API_KEY_1: ${{ secrets.GOOGLE_API_KEY_1 }}
          GOOGLE_API_KEY_2: ${{ secrets.GOOGLE_API_KEY_2 }}
          GOOGLE_API_KEY_3: ${{ secrets.GOOGLE_API_KEY_3 }}
          GOOGLE_API_KEY_4: ${{ secrets.GOOGLE_API_KEY_4 }}
          GOOGLE_API_KEY_5: ${{ secrets.GOOGLE_API_KEY_5 }}
          GOOGLE_API_KEY_6: ${{ secrets.GOOGLE_API_KEY_6 }}
          GOOGLE_API_KEY_7: ${{ secrets.GOOGLE_API_KEY_7 }}
          GOOGLE_API_KEY_8: ${{ secrets.GOOGLE_API_KEY_8 }}
          GOOGLE_API_KEY_9: ${{ secrets.GOOGLE_API_KEY_9 }}
          GOOGLE_API_KEY_10: ${{ secrets.GOOGLE_API_KEY_10 }}
          GOOGLE_API_KEY_11: ${{ secrets.GOOGLE_API_KEY_11 }}
          GOOGLE_API_KEY_12: ${{ secrets.GOOGLE_API_KEY_12 }}
          GOOGLE_API_KEY_13: ${{ secrets.GOOGLE_API_KEY_13 }}
          GOOGLE_API_KEY_14: ${{ secrets.GOOGLE_API_KEY_14 }}
          GOOGLE_API_KEY_15: ${{ secrets.GOOGLE_API_KEY_15 }}
          GOOGLE_API_KEY_16: ${{ secrets.GOOGLE_API_KEY_16 }}
          GOOGLE_API_KEY_17: ${{ secrets.GOOGLE_API_KEY_17 }}
          GOOGLE_API_KEY_18: ${{ secrets.GOOGLE_API_KEY_18 }}
          GOOGLE_API_KEY_19: ${{ secrets.GOOGLE_API_KEY_19 }}
          GOOGLE_API_KEY_20: ${{ secrets.GOOGLE_API_KEY_20 }}
          GOOGLE_API_KEY_21: ${{ secrets.GOOGLE_API_KEY_21 }}
          GOOGLE_API_KEY_22: ${{ secrets.GOOGLE_API_KEY_22 }}
          GOOGLE_API_KEY_23: ${{ secrets.GOOGLE_API_KEY_23 }}
          GOOGLE_API_KEY_24: ${{ secrets.GOOGLE_API_KEY_24 }}
          GOOGLE_API_KEY_25: ${{ secrets.GOOGLE_API_KEY_25 }}
          GOOGLE_API_KEY_26: ${{ secrets.GOOGLE_API_KEY_26 }}
          GOOGLE_API_KEY_27: ${{ secrets.GOOGLE_API_KEY_27 }}
          GOOGLE_API_KEY_28: ${{ secrets.GOOGLE_API_KEY_28 }}
          GOOGLE_API_KEY_29: ${{ secrets.GOOGLE_API_KEY_29 }}
          GOOGLE_API_KEY_30: ${{ secrets.GOOGLE_API_KEY_30 }}
        run: |
          echo "🚀 Démarrage de la génération des rapports..."
          echo "📊 Utilisation des clés non épuisées par l'étape 4"
          python report_generator.py
          echo "✅ Rapports générés"
      
      - name: 📤 Upload des Rapports générés
        uses: actions/upload-artifact@v4
        with:
          name: rapports-brvm-${{ github.run_number }}
          path: |
            Rapport_*.docx
          retention-days: 90
      
      - name: 📊 Résumé de l'exécution
        if: always()
        run: |
          echo "═══════════════════════════════════════════════════════════"
          echo "📈 RÉSUMÉ DE L'EXÉCUTION V9.1 (30 CLÉS API - CORRIGÉE)"
          echo "═══════════════════════════════════════════════════════════"
          echo "Date: $(date '+%Y-%m-%d %H:%M:%S UTC')"
          echo "Run #: ${{ github.run_number }}"
          echo "Base de données: Supabase (PostgreSQL)"
          echo "Clés API Gemini: 30 clés disponibles"
          echo "Capacité théorique: 450 requêtes/minute (30 x 15)"
          echo "Capacité quotidienne: ~45,000 requêtes/jour"
          echo "═══════════════════════════════════════════════════════════"
          
          if [ -f "Rapport_Synthese_Investissement_BRVM_"*.docx ]; then
            echo "✅ Rapports générés avec succès"
            ls -lh Rapport_*.docx
          else
            echo "⚠️  Aucun rapport trouvé"
          fi
          
          echo "═══════════════════════════════════════════════════════════"
          echo "📊 CORRECTIONS APPLIQUÉES:"
          echo "   • Installation Chrome modernisée"
          echo "   • Selenium-wire v5.x compatible"
          echo "   • Validation réponses API renforcée"
          echo "   • Versions dépendances épinglées"
          echo "═══════════════════════════════════════════════════════════"
